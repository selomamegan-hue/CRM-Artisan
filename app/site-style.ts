/* Polices et feuille de style communes à la page d'accueil et à la
   bibliothèque /fonctions. Extraites du document porté pour ne pas être
   dupliquées d'une page à l'autre. */

export const SITE_STYLE = "<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin>\n<link href=\"https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,300;9..144,400;9..144,500;9..144,600;9..144,700&family=Inter:wght@400;500;600;700;800&display=swap\" rel=\"stylesheet\">\n<style>\n  :root{\n    --blue:#1A5F7A; --blue-bright:#2C7FA0;\n    --emerald:#3A9188; --terracotta:#D97B4F; --terracotta-deep:#C1633A;\n    --paper:#F7F3EC; --paper-2:#F1ECE2; --paper-3:#EAE1CE;\n    --ink:#22303A; --ink-soft:#5B6B72; --ink-faint:#8B9298;\n\n    --bg:var(--paper); --bg-elevated:#FFFFFF; --bg-sunken:#1B2530;\n    --text:var(--ink); --text-soft:var(--ink-soft); --text-faint:var(--ink-faint);\n    --text-on-sunken:#F5EFE3; --text-on-sunken-soft:rgba(245,239,227,0.68);\n    --accent:var(--terracotta); --accent-deep:var(--terracotta-deep);\n    --accent-blue:var(--blue); --accent-emerald:var(--emerald);\n    --border:rgba(34,48,58,0.12); --border-on-sunken:rgba(245,239,227,0.16);\n    --shadow:0 1px 2px rgba(34,48,58,0.05), 0 16px 40px rgba(34,48,58,0.09);\n    --shadow-lg:0 4px 10px rgba(34,48,58,0.08), 0 30px 60px rgba(34,48,58,0.16);\n  }\n\n  @media (prefers-color-scheme: dark){\n    :root:not([data-theme=\"light\"]){\n      --bg:#14191D; --bg-elevated:#1D242A; --bg-sunken:#0B0F12;\n      --text:#F2ECE0; --text-soft:#B7C0C4; --text-faint:#8B959A;\n      --accent:#E08957; --accent-deep:#D97B4F;\n      --accent-blue:#3E93B8; --accent-emerald:#4CAE9F;\n      --border:rgba(242,236,224,0.12); --border-on-sunken:rgba(245,239,227,0.14);\n      --shadow:0 1px 2px rgba(0,0,0,0.35), 0 16px 40px rgba(0,0,0,0.45);\n      --shadow-lg:0 4px 10px rgba(0,0,0,0.4), 0 30px 60px rgba(0,0,0,0.55);\n    }\n  }\n  :root[data-theme=\"dark\"]{\n    --bg:#14191D; --bg-elevated:#1D242A; --bg-sunken:#0B0F12;\n    --text:#F2ECE0; --text-soft:#B7C0C4; --text-faint:#8B959A;\n    --accent:#E08957; --accent-deep:#D97B4F;\n    --accent-blue:#3E93B8; --accent-emerald:#4CAE9F;\n    --border:rgba(242,236,224,0.12); --border-on-sunken:rgba(245,239,227,0.14);\n    --shadow:0 1px 2px rgba(0,0,0,0.35), 0 16px 40px rgba(0,0,0,0.45);\n    --shadow-lg:0 4px 10px rgba(0,0,0,0.4), 0 30px 60px rgba(0,0,0,0.55);\n  }\n\n  *{box-sizing:border-box;}\n  html{scroll-behavior:smooth;}\n  body{\n    margin:0; background:var(--bg); color:var(--text);\n    font-family:'Inter', system-ui, -apple-system, sans-serif;\n    font-size:16px; line-height:1.6; -webkit-font-smoothing:antialiased;\n    overflow-x:hidden;\n  }\n  h1,h2,h3{ font-family:'Fraunces', Georgia, serif; text-wrap:balance; margin:0; color:var(--text); }\n  p{ margin:0; }\n  a{ color:inherit; }\n  img,svg{ display:block; max-width:100%; }\n  ::selection{ background:var(--accent); color:#fff; }\n  :focus-visible{ outline:2.5px solid var(--accent-blue); outline-offset:3px; border-radius:3px; }\n\n  .wrap{ max-width:1160px; margin:0 auto; padding-inline:clamp(20px,5vw,56px); }\n  section{ padding-block:clamp(64px,10vw,120px); }\n\n  .eyebrow{\n    font-size:12px; font-weight:700; letter-spacing:0.14em; text-transform:uppercase;\n    color:var(--accent-deep); margin-bottom:14px; display:flex; align-items:center; gap:10px;\n  }\n  .eyebrow::before{ content:\"\"; width:22px; height:1.5px; background:var(--accent); display:inline-block; }\n  .eyebrow.on-sunken{ color:#EDA97D; }\n  .eyebrow.on-sunken::before{ background:#EDA97D; }\n\n  /* ---------- NAV ---------- */\n  .nav{\n    position:sticky; top:0; z-index:40; backdrop-filter:blur(10px);\n    background:color-mix(in srgb, var(--bg) 82%, transparent);\n    border-bottom:1px solid var(--border);\n  }\n  .nav-inner{ display:flex; align-items:center; justify-content:space-between; padding-block:16px; }\n  .brand{ display:flex; align-items:center; gap:9px; font-family:'Fraunces',serif; font-weight:600; font-size:20px; letter-spacing:-0.01em; text-decoration:none; }\n  .brand-mark{ width:26px; height:36px; object-fit:contain; flex-shrink:0; display:block; }\n  .nav-links{ display:flex; align-items:center; gap:clamp(18px,3vw,34px); font-size:14px; font-weight:500; color:var(--text-soft); }\n  .nav-links a{ text-decoration:none; transition:color .15s; }\n  .nav-links a:hover{ color:var(--text); }\n  .nav-cta{\n    background:var(--ink); color:var(--paper); padding:9px 18px; border-radius:8px;\n    font-weight:600; font-size:13.5px; text-decoration:none; white-space:nowrap;\n  }\n  :root[data-theme=\"dark\"] .nav-cta, @media (prefers-color-scheme:dark){ :root:not([data-theme=\"light\"]) .nav-cta{ background:var(--accent); color:#1B120B; } }\n  .nav-mobile-hide{ display:flex; }\n  @media (max-width:720px){ .nav-mobile-hide{ display:none; } }\n\n  /* ---------- HERO ---------- */\n  .hero{ padding-block:clamp(56px,9vw,104px) clamp(48px,8vw,88px); position:relative; }\n  .hero-grid{ display:grid; grid-template-columns:1.05fr 0.95fr; gap:clamp(32px,5vw,64px); align-items:center; }\n  .hero-grid > *{ min-width:0; }\n  @media (max-width:940px){ .hero-grid{ grid-template-columns:1fr; } }\n  .hero h1{ font-size:clamp(2.3rem,4.6vw + 0.6rem,3.9rem); font-weight:600; line-height:1.06; letter-spacing:-0.015em; }\n  .hero h1 em{ font-style:italic; color:var(--accent-deep); }\n  .hero-sub{ margin-top:22px; font-size:clamp(16px,1.6vw,18.5px); color:var(--text-soft); max-width:46ch; line-height:1.65; }\n  .hero-actions{ margin-top:32px; display:flex; align-items:center; gap:20px; flex-wrap:wrap; }\n  .btn{\n    display:inline-flex; align-items:center; gap:9px; padding:14px 26px; border-radius:10px;\n    font-weight:700; font-size:15px; text-decoration:none; border:1.5px solid transparent; cursor:pointer;\n    transition:transform .15s ease, box-shadow .15s ease, background .15s ease; font-family:inherit;\n  }\n  .btn:active{ transform:translateY(1px); }\n  .btn-primary{ background:var(--accent); color:#fff; box-shadow:0 10px 24px -6px rgba(217,123,79,0.55); }\n  .btn-primary:hover{ background:var(--accent-deep); box-shadow:0 14px 30px -6px rgba(217,123,79,0.6); }\n  .btn-ghost-link{ color:var(--text); font-weight:600; font-size:14.5px; text-decoration:none; display:inline-flex; align-items:center; gap:6px; border-bottom:1.5px solid var(--border); padding-bottom:2px; }\n  .btn-ghost-link:hover{ border-color:var(--text); }\n  .hero-trust{ margin-top:18px; font-size:13px; color:var(--text-faint); }\n\n  /* waveform -> card graphic */\n  .hero-art{ position:relative; display:flex; align-items:center; justify-content:center; min-height:280px; }\n  .phone-frame{\n    position:relative; width:min(280px,78vw); aspect-ratio:338/600; border-radius:34px; overflow:hidden;\n    box-shadow:var(--shadow-lg); background:var(--ink);\n  }\n  .phone-frame::before{ content:\"\"; position:absolute; inset:0; border-radius:34px; border:1px solid rgba(255,255,255,0.14); z-index:2; pointer-events:none; }\n  .phone-frame video{ width:100%; height:100%; object-fit:cover; display:block; }\n  .hero-art-glow{ position:absolute; top:-50px; right:calc(50% - 210px); width:170px; height:170px; border-radius:50%; background:radial-gradient(circle, rgba(217,123,79,0.22), transparent 70%); z-index:-1; }\n\n  /* ---------- SUNKEN / PROBLEM ---------- */\n  .sunken{ background:var(--bg-sunken); color:var(--text-on-sunken); }\n  .sunken h2{ color:var(--text-on-sunken); }\n  .sunken .lede{ color:var(--text-on-sunken-soft); font-size:clamp(16px,1.4vw,18px); max-width:62ch; line-height:1.7; margin-top:18px; }\n  .pain-list{ margin-top:44px; display:grid; gap:0; border-top:1px solid var(--border-on-sunken); }\n  .pain-item{\n    display:grid; grid-template-columns:auto 1fr; gap:20px; padding-block:24px;\n    border-bottom:1px solid var(--border-on-sunken); align-items:baseline;\n  }\n  .pain-mark{ font-family:'Fraunces',serif; font-style:italic; font-size:15px; color:#EDA97D; }\n  .pain-item p{ font-size:clamp(16px,1.3vw,18.5px); color:var(--text-on-sunken); line-height:1.55; max-width:60ch; }\n  .sunken-close{ margin-top:40px; font-family:'Fraunces',serif; font-style:italic; font-size:clamp(19px,1.9vw,24px); color:var(--text-on-sunken); max-width:52ch; line-height:1.5; }\n\n  /* ---------- SOLUTION ---------- */\n  .section-head{ max-width:62ch; }\n  .section-head h2{ font-size:clamp(1.9rem,2.6vw + 0.6rem,2.7rem); font-weight:600; letter-spacing:-0.01em; }\n  .section-head .lede{ margin-top:16px; font-size:16.5px; color:var(--text-soft); max-width:56ch; }\n\n  .feat-grid{ margin-top:48px; display:grid; grid-template-columns:repeat(3,1fr); gap:1px; background:var(--border); border:1px solid var(--border); border-radius:16px; overflow:hidden; }\n  .feat-grid > *{ min-width:0; }\n  @media (max-width:860px){ .feat-grid{ grid-template-columns:repeat(2,1fr); } }\n  @media (max-width:560px){ .feat-grid{ grid-template-columns:1fr; } }\n  .feat{ background:var(--bg-elevated); padding:30px 26px; }\n  .feat-icon{ width:38px; height:38px; border-radius:10px; display:flex; align-items:center; justify-content:center; margin-bottom:18px; }\n  .feat-icon svg{ width:19px; height:19px; }\n  .feat h3{ font-size:17.5px; font-weight:600; margin-bottom:9px; }\n  .feat p{ font-size:14px; color:var(--text-soft); line-height:1.6; }\n\n  /* ---------- SECURITE ---------- */\n  .security{ padding-block:clamp(52px,7vw,84px); border-top:1px solid var(--border); border-bottom:1px solid var(--border); }\n  .security-grid{ display:grid; grid-template-columns:0.9fr 1.4fr; gap:clamp(28px,4vw,56px); align-items:start; }\n  .security-grid > *{ min-width:0; }\n  @media (max-width:820px){ .security-grid{ grid-template-columns:1fr; } }\n  .security-points{ display:grid; grid-template-columns:repeat(3,1fr); gap:28px; }\n  .security-points > *{ min-width:0; }\n  @media (max-width:820px){ .security-points{ grid-template-columns:1fr; gap:22px; } }\n  .security-point .sp-icon{ width:34px; height:34px; border-radius:9px; background:color-mix(in srgb, var(--accent-blue) 14%, transparent); display:flex; align-items:center; justify-content:center; margin-bottom:14px; }\n  .security-point .sp-icon svg{ width:17px; height:17px; }\n  .security-point h3{ font-size:15px; font-weight:600; margin-bottom:7px; }\n  .security-point p{ font-size:13.5px; color:var(--text-soft); line-height:1.6; }\n\n  /* ---------- FAQ ---------- */\n  .faq-list{ margin-top:44px; max-width:74ch; border-top:1px solid var(--border); }\n  .faq-item{ border-bottom:1px solid var(--border); }\n  .faq-item summary{\n    cursor:pointer; list-style:none; padding-block:22px; display:flex; align-items:center; justify-content:space-between;\n    gap:20px; font-family:'Fraunces',serif; font-size:17px; font-weight:500; color:var(--text);\n  }\n  .faq-item summary::-webkit-details-marker{ display:none; }\n  .faq-item summary .faq-plus{ flex-shrink:0; width:22px; height:22px; position:relative; }\n  .faq-item summary .faq-plus::before, .faq-item summary .faq-plus::after{\n    content:\"\"; position:absolute; background:var(--text-soft); border-radius:2px; transition:transform .2s ease;\n  }\n  .faq-item summary .faq-plus::before{ top:50%; left:0; width:100%; height:1.6px; transform:translateY(-50%); }\n  .faq-item summary .faq-plus::after{ left:50%; top:0; height:100%; width:1.6px; transform:translateX(-50%); }\n  .faq-item[open] summary .faq-plus::after{ transform:translateX(-50%) rotate(90deg); opacity:0; }\n  .faq-item p.faq-a{ margin:0 0 22px; padding-right:42px; font-size:14.5px; color:var(--text-soft); line-height:1.65; max-width:60ch; }\n\n  /* ---------- PRICING ---------- */\n  .price-top{ display:flex; justify-content:space-between; align-items:flex-end; gap:24px; flex-wrap:wrap; }\n  .trial-pill{\n    display:inline-flex; align-items:center; gap:8px; background:color-mix(in srgb, var(--accent-emerald) 12%, transparent);\n    color:var(--accent-emerald); font-weight:700; font-size:13px; padding:9px 16px; border-radius:30px; white-space:nowrap;\n  }\n  :root[data-theme=\"dark\"] .trial-pill, @media (prefers-color-scheme:dark){:root:not([data-theme=\"light\"]) .trial-pill{ color:#7CCFC0; }}\n  .price-grid{ margin-top:46px; display:grid; grid-template-columns:repeat(3,1fr); gap:22px; align-items:stretch; }\n  .price-grid > *{ min-width:0; }\n  @media (max-width:900px){ .price-grid{ grid-template-columns:1fr; max-width:440px; margin-inline:auto; } }\n  .price-card{\n    background:var(--bg-elevated); border:1px solid var(--border); border-radius:18px; padding:30px 26px;\n    display:flex; flex-direction:column; box-shadow:var(--shadow);\n  }\n  .price-card.featured{ border-color:var(--accent); box-shadow:var(--shadow-lg); position:relative; transform:translateY(-8px); }\n  @media (max-width:900px){ .price-card.featured{ transform:none; } }\n  .price-tag{ position:absolute; top:-13px; left:26px; background:var(--accent); color:#fff; font-size:11.5px; font-weight:700; letter-spacing:0.03em; padding:5px 12px; border-radius:20px; }\n  .price-name{ font-family:'Fraunces',serif; font-weight:600; font-size:21px; }\n  .price-tagline{ font-size:13px; color:var(--text-soft); margin-top:5px; min-height:34px; }\n  .price-amount{ margin-top:20px; display:flex; align-items:baseline; gap:6px; font-variant-numeric:tabular-nums; }\n  .price-amount .num{ font-family:'Fraunces',serif; font-size:32px; font-weight:600; }\n  .price-amount .unit{ font-size:13.5px; color:var(--text-soft); font-weight:500; }\n  .price-annual{ margin-top:4px; font-size:12.5px; color:var(--text-faint); font-variant-numeric:tabular-nums; }\n  .price-feats{ margin-top:22px; padding-top:22px; border-top:1px solid var(--border); display:flex; flex-direction:column; gap:11px; flex:1; }\n  .price-feats li{ list-style:none; display:flex; align-items:flex-start; gap:9px; font-size:13.5px; color:var(--text); line-height:1.5; }\n  .price-feats li::before{ content:\"✓\"; color:var(--accent-emerald); font-weight:700; flex-shrink:0; margin-top:1px; }\n  .price-cta{ margin-top:26px; text-align:center; padding:12px; border-radius:9px; font-weight:700; font-size:14px; text-decoration:none; border:1.5px solid var(--ink); color:var(--text); }\n  .price-card.featured .price-cta{ background:var(--accent); border-color:var(--accent); color:#fff; }\n  .payment-note{ margin-top:36px; font-size:13px; color:var(--text-faint); text-align:center; }\n\n  /* ---------- CONTACT ---------- */\n  .contact-grid{ display:grid; grid-template-columns:0.85fr 1.15fr; gap:clamp(30px,5vw,72px); align-items:start; }\n  .contact-grid > *{ min-width:0; }\n  @media (max-width:880px){ .contact-grid{ grid-template-columns:1fr; } }\n  .contact-side h2{ font-size:clamp(1.9rem,2.6vw + 0.6rem,2.6rem); }\n  .contact-side .lede{ margin-top:16px; color:var(--text-soft); font-size:16px; max-width:42ch; line-height:1.65; }\n  .contact-alt{ margin-top:30px; display:flex; flex-direction:column; gap:10px; }\n  .contact-alt a{ text-decoration:none; font-weight:600; font-size:14.5px; display:flex; align-items:center; gap:9px; color:var(--text); }\n  .contact-alt .ico{ width:30px; height:30px; border-radius:8px; background:var(--paper-2); display:flex; align-items:center; justify-content:center; flex-shrink:0; }\n  :root[data-theme=\"dark\"] .contact-alt .ico, @media (prefers-color-scheme:dark){:root:not([data-theme=\"light\"]) .contact-alt .ico{ background:var(--bg); }}\n\n  form.card{ background:var(--bg-elevated); border:1px solid var(--border); border-radius:18px; padding:clamp(24px,3vw,34px); box-shadow:var(--shadow); }\n  .field{ display:flex; flex-direction:column; gap:7px; margin-bottom:18px; }\n  .field label{ font-size:12px; font-weight:700; letter-spacing:0.03em; text-transform:uppercase; color:var(--text-soft); }\n  .field input, .field select, .field textarea{\n    font-family:inherit; font-size:15px; padding:12px 14px; border-radius:9px; border:1.5px solid var(--border);\n    background:var(--bg); color:var(--text); outline:none; transition:border-color .15s;\n  }\n  .field input:focus, .field select:focus, .field textarea:focus{ border-color:var(--accent-blue); }\n  .field-row{ display:grid; grid-template-columns:1fr 1fr; gap:14px; }\n  @media (max-width:480px){ .field-row{ grid-template-columns:1fr; } }\n  textarea{ resize:vertical; min-height:84px; }\n  .form-submit{ width:100%; justify-content:center; margin-top:6px; }\n  .form-note{ margin-top:14px; font-size:12.5px; color:var(--text-faint); text-align:center; }\n  .form-status{ margin-top:14px; font-size:13.5px; text-align:center; font-weight:600; min-height:0; }\n  .form-status.show{ padding:10px; border-radius:8px; background:color-mix(in srgb, var(--accent-emerald) 12%, transparent); color:var(--accent-emerald); }\n  .form-status a{ text-decoration:underline; }\n\n  /* ---------- FOOTER ---------- */\n  footer{ border-top:1px solid var(--border); padding-block:40px; }\n  .footer-inner{ display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:16px; font-size:13px; color:var(--text-faint); }\n  .footer-inner .brand{ font-size:16px; }\n  .footer-tagline{ margin-top:16px; font-family:'Fraunces',serif; font-style:italic; font-size:13px; color:var(--accent-emerald); text-align:center; }\n</style><style>\n    /* ---------- FONCTIONS EN IMAGES ---------- */\n    .fn-grid{ margin-top:48px; display:grid; grid-template-columns:repeat(5,1fr); gap:clamp(14px,2vw,26px); }\n    .fn-grid > *{ min-width:0; }\n    @media (max-width:1000px){ .fn-grid{ grid-template-columns:repeat(3,1fr); } }\n    @media (max-width:640px){ .fn-grid{ grid-template-columns:repeat(2,1fr); } }\n    .fn-poster{\n      position:relative; display:block; width:100%; aspect-ratio:9/16; padding:0; cursor:pointer;\n      border:1px solid var(--border); border-radius:18px; overflow:hidden; background:var(--paper-2);\n      box-shadow:var(--shadow); transition:transform .18s ease, box-shadow .18s ease;\n    }\n    .fn-poster:hover{ transform:translateY(-4px); box-shadow:var(--shadow-lg); }\n    .fn-poster:active{ transform:translateY(-1px); }\n    .fn-thumb{ position:absolute; inset:0; display:flex; align-items:center; justify-content:center; }\n    .fn-thumb svg{ width:118%; height:118%; }\n    .fn-play{\n      position:absolute; left:50%; top:50%; transform:translate(-50%,-50%);\n      width:clamp(42px,4.4vw,56px); height:clamp(42px,4.4vw,56px); border-radius:50%;\n      background:var(--accent); color:#fff; display:flex; align-items:center; justify-content:center;\n      box-shadow:0 10px 26px -6px rgba(217,123,79,0.7); transition:background .15s ease, transform .15s ease;\n    }\n    .fn-play svg{ width:46%; height:46%; margin-left:6%; }\n    .fn-poster:hover .fn-play{ background:var(--accent-deep); transform:translate(-50%,-50%) scale(1.06); }\n    .fn-dur{\n      position:absolute; right:9px; bottom:9px; padding:3px 8px; border-radius:20px;\n      background:rgba(34,48,58,0.78); color:#F5EFE3; font-size:11.5px; font-weight:600;\n      font-variant-numeric:tabular-nums; letter-spacing:0.02em;\n    }\n    .fn-card h3{ margin-top:15px; font-size:16px; font-weight:600; line-height:1.3; }\n    .fn-card .fn-num{ color:var(--accent-deep); }\n    .fn-card p{ margin-top:6px; font-size:13.5px; color:var(--text-soft); line-height:1.55; }\n    .fn-note{ margin-top:34px; font-size:13px; color:var(--text-faint); }\n\n    /* ---------- LECTEUR PLEIN ÉCRAN ---------- */\n    .om-modal{\n      position:fixed; inset:0; z-index:120; display:none;\n      align-items:center; justify-content:center; padding:clamp(12px,3vw,32px);\n      background:rgba(11,15,18,0.86); backdrop-filter:blur(6px);\n    }\n    .om-modal.open{ display:flex; }\n    .om-dialog{ position:relative; display:flex; flex-direction:column; align-items:center; gap:12px; max-height:100%; }\n    .om-frame{ aspect-ratio:1080/2010; height:min(86vh,900px); width:auto; max-width:94vw; }\n    @media (max-width:560px){ .om-frame{ height:auto; width:94vw; max-height:82vh; } }\n    .om-frame iframe{ width:100%; height:100%; border:0; border-radius:18px; display:block; background:#22303A; box-shadow:var(--shadow-lg); }\n    .om-bar{ display:flex; align-items:center; gap:14px; color:#F5EFE3; font-size:13.5px; }\n    .om-bar strong{ font-family:'Fraunces',serif; font-weight:500; font-size:15px; }\n    .om-close{\n      display:inline-flex; align-items:center; gap:7px; padding:8px 15px; border-radius:9px; cursor:pointer;\n      background:rgba(245,239,227,0.12); border:1px solid rgba(245,239,227,0.22); color:#F5EFE3;\n      font-family:inherit; font-size:13px; font-weight:600;\n    }\n    .om-close:hover{ background:rgba(245,239,227,0.2); }\n  \n  /* ---------- BOUTON CONTACT ---------- */\n  .contact-cta{ margin-top:26px; display:flex; justify-content:center; }\n  .contact-cta a{\n    display:inline-flex; align-items:center; gap:9px; padding:13px 26px; border-radius:10px;\n    font-weight:700; font-size:14.5px; text-decoration:none; font-family:inherit;\n    border:1.5px solid var(--border); color:var(--text); background:var(--bg-elevated);\n    transition:border-color .15s ease, transform .15s ease;\n  }\n  .contact-cta a:hover{ border-color:var(--text); }\n  .contact-cta a:active{ transform:translateY(1px); }\n\n  /* ---------- ACTIONS DU MENU (toujours visibles, même sur mobile) ---------- */\n  .nav-actions{ display:flex; align-items:center; gap:16px; flex-shrink:0; }\n  .nav-login{\n    font-size:13.5px; font-weight:600; color:var(--text-soft);\n    text-decoration:none; white-space:nowrap; transition:color .15s;\n  }\n  .nav-login:hover{ color:var(--text); }\n  @media (max-width:420px){\n    .nav-actions{ gap:11px; }\n    .nav-login{ font-size:13px; }\n    .nav-cta{ padding:8px 14px; font-size:13px; }\n  }\n\n  /* Le halo du hero est calé sur une mise en page à deux colonnes :\n     right:calc(50% - 210px) passe en négatif sur écran étroit et déborde\n     de la page. Sous 940px la colonne est unique — on le recentre. */\n  @media (max-width:940px){\n    .hero-art-glow{ right:auto; left:50%; transform:translateX(-50%); }\n  }\n</style><style>\n  /* ---------- BIBLIOTHÈQUE : fonctions sans film ---------- */\n  .fn-card.fn-soon .fn-thumb{ background:var(--paper-2); display:flex; align-items:center; justify-content:center; }\n  .fn-card.fn-soon .fn-thumb span{\n    font-family:'Fraunces',serif; font-size:34px; font-weight:600; color:var(--text-faint);\n  }\n  .fn-soon-tag{\n    position:absolute; left:12px; bottom:12px; background:color-mix(in srgb, var(--ink) 72%, transparent);\n    color:var(--paper); font-size:11.5px; font-weight:700; letter-spacing:0.04em;\n    padding:5px 10px; border-radius:20px;\n  }\n  .lib-head{ padding-block:clamp(48px,7vw,84px) 0; }\n  .lib-head h1{ font-size:clamp(2rem,3vw + 0.6rem,3rem); font-weight:600; letter-spacing:-0.015em; }\n  .lib-group{ margin-top:52px; }\n  .lib-group > .eyebrow{ margin-bottom:22px; }\n  .lib-back{ display:inline-flex; align-items:center; gap:8px; font-size:14px; font-weight:600;\n    color:var(--text-soft); text-decoration:none; }\n  .lib-back:hover{ color:var(--text); }\n  .fn-more{ margin-top:34px; display:flex; justify-content:center; }\n  .fn-more a{ display:inline-flex; align-items:center; gap:9px; padding:13px 26px; border-radius:10px;\n    font-weight:700; font-size:14.5px; text-decoration:none;\n    border:1.5px solid var(--border); color:var(--text); background:var(--bg-elevated); }\n  .fn-more a:hover{ border-color:var(--text); }\n</style>"
